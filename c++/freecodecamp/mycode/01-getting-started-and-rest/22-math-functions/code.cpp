#include <iosteam>
#include <cmath>


int main() {


    double weight {7.7};

    //floor 


    /*
    std::floor()
    std::ceil()
std::abs()
std::exp() => e^x
std::pow(a,b) => a^b
std::log(var) => ln(var)
std::log10(var) ==> log(var)
std::round(var) => // var ı integer a çeviriyor 0.5 in üstü yukarı yuvarlıyor.
//trigonometic functins
    */
    return 0;
}